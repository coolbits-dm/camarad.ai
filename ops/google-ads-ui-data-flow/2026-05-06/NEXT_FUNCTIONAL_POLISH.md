# Next Functional Polish

Date: 2026-05-06

## Recommended Order

1. Source badges everywhere in remaining legacy panels
   - Finish normalizing labels in hidden/legacy Google Ads panes.
   - Include date/currency/source metadata near every rendered table.

2. Search Terms polish
   - Improve table scanning and warning placement.
   - Keep standard `search_term_view` limitation explicit.
   - Do not add negative keyword actions until there is an approval flow.

3. Account Health Score v1
   - Convert existing deterministic signals into a transparent score.
   - Keep scoring explainable and sourced from live/fallback-labeled data.

4. PMax module
   - Add a separate Performance Max path only after source policy is defined.
   - Do not mix `search_term_view` and PMax term coverage.

5. AI Ask
   - Add only after the data source contract is visible and stable.
   - Responses should cite which panel data and source labels were used.
